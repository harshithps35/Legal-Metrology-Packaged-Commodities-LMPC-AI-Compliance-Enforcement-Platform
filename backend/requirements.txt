# LMPC Compliance System — Backend Dependencies
# Python 3.10+

# === Web Framework ===
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# === Database ===
sqlalchemy==2.0.36
alembic==1.14.1
psycopg2-binary==2.9.10          # PostgreSQL driver (swap for aiosqlite if using SQLite)
aiosqlite==0.20.0                 # SQLite async driver for local dev/demo

# === Authentication ===
python-jose[cryptography]==3.3.0  # JWT token handling
passlib[bcrypt]==1.7.4            # Password hashing
bcrypt==4.2.1

# === OCR & Computer Vision ===
opencv-python-headless==4.10.0.84
pytesseract==0.3.13
numpy==2.2.1
Pillow==11.1.0
winocr>=0.0.8; sys_platform == 'win32'     # Windows native OCR fallback (skipped on Linux CI)

# === NLP & Text Processing ===
spacy==3.8.3
rapidfuzz==3.11.0

# === PDF & Document Export ===
reportlab==4.2.5
python-docx==1.1.2
openpyxl==3.1.5
qrcode[pil]==8.0

# === Utilities ===
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1
aiofiles==24.1.0
slowapi==0.1.9                    # Rate limiting middleware

# === Testing ===
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0

# === Dev Tools ===
ruff==0.8.6
